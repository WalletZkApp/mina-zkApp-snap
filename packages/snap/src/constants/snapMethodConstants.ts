export enum ESnapMethod {
  SnapDialog = 'snap_dialog',
  SnapGetBip32Entropy = 'snap_getBip32Entropy',
  SnapNotify = 'snap_notify',
  SnapManageState = 'snap_manageState',
}

export enum ESnapDialogType {
  Alert = 'alert',
  Confirmation = 'confirmation',
  Prompt = 'prompt',
}
